# Application environment: development, test, or production
NODE_ENV=development

# HTTP port exposed by the application
PORT=3000

# PostgreSQL connection URL without the password
DB_URL=postgresql://app_user@127.0.0.1:21432/marketplace

# Path to the file containing the PostgreSQL password
DB_PASSWORD_FILE=secrets/db_password
